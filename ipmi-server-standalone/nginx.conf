server {
    listen 80;
    server_name localhost;
    root /var/www/html/public;
    index index.php index.html;

    # Primary location for your PHP app
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Pass PHP scripts to FastCGI server (PHP-FPM)
    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\\.php)(/.+)$;
        # PHP-FPM by default listens on port 9000
        fastcgi_pass 127.0.0.1:9000; 
        fastcgi_index index.php;
        include fastcgi_params;
        
        # Match add-on budgets: ipmitool can take a while under auto-detect.
        fastcgi_read_timeout 120s;
        fastcgi_send_timeout 120s;
        # Critical for PHP to find your files
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    # Deny access to sensitive files
    location ~ /\.(ht|git|conf) {
        deny all;
    }
}
